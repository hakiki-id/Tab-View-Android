<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>



    <style name="TextLabel" parent="TextAppearance.AppCompat">
        <!-- hint Color and label color in false state-->
        <item name="android:textColorHint">@color/hint_label</item>
        <item name="android:textSize">20sp</item>

        <!--label color in true state and bar color false and true state-->
        <item name="android:colorAccent">@color/accent_label</item>
        <item name="colorControlNormal">@color/normal_label</item>
        <item name="colorControlActivated">@color/activated_label</item>
    </style>

</resources>
